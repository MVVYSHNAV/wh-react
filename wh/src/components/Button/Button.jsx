const Button = () => {
  return (
    <div className="relative z-20 mx-auto flex justify-center">
      <button className="rounded-full bg-gradient-to-b from-pink-600 to-black px-8 py-3 font-semibold text-white shadow-lg hover:text-gray-400 drop-shadow-3xl opacity-100">
        Get a Seat
      </button>
    </div>
  );
};

export default Button;
